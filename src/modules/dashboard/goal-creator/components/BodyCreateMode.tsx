import { useGoalsStore } from '@/StoreProvider'
import { observer } from 'mobx-react-lite'

export const BodyCreateMode: React.FC = observer(() => {
   
    return <div>Edit Mode or Create Mode</div>
})
